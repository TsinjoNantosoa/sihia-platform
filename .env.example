# SIH IA — Configuration d'environnement
# Copier ce fichier en .env puis adapter les valeurs.

# URL du backend FastAPI (laisser vide pour utiliser les mocks intégrés)
VITE_API_URL=http://localhost:8000

# Activer/désactiver le mode mock (true = mocks, false = appels réels)
VITE_USE_MOCKS=false
